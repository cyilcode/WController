using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace WCS.MAIN.Globals
{
    public class Settings
    {
        /*Git connection test. Seems like my connection here is a bit shaky.*/
        private const string OSX_CONF_FILE = "";
        private const string WIN_CONF_FILE = "";
        private const string LIN_CONF_FILE = "";
        public Settings(OS platform)
        {

        }
    }
}
